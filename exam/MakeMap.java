// create a Count class with one static method called count which use templates and counts the number of times the item occurs in an array.
// The method should have two parameters, the first is the array and the second is the item to be counted.
import java.util.*;

public class MakeMap
{
    // No main method required.

    public static Map countLetters(String word)
    {
        Map<String, Integer> map = new HashMap<>();
        char [] chars = word.toCharArray();

        for(int i=0; i<word.length(); i++)
        {
            int count=0;

            
        }

        return map;
    }
}